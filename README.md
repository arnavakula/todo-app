# todo-app
1. Create the virtual environment: "python3 -m venv venv"
2. Install all dependencies: "pip install -r requirements.txt"
3. Start running backend, first by navigating to backend folder: "uvicorn app:app --reload"
4. Start running frontend, first by navigating to frontend folder: "npm run dev"
5. Open http://localhost:3000
